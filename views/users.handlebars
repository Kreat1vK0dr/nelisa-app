<h1>USERS</h1>

<center>
<table class="myTables">
  <thead>
    <tr>
      <th>User ID</th>
      <th>User Name</th>
      <th>Role</th>
      <th>Admin Role</th>
    </tr>
  </thead>
  <tbody>
    {{#users}}
    <tr>
    <td>{{id}}</td>
    <td>{{username}}</td>
    <td>{{role}}</td>
    <td>{{admin_role}}</td>
    <td>{{date_added}}</td>
    <td>{{last_login}}</td>
    <td class="crud-table highlight">
    <form action="/uses/edit">
      <input type="hidden" name="id" value="{{id}}">
      <input type="hidden" name="username" value="{{username}}">
      <input type="hidden" name="rold" value="{{role}}">
      <input type="hidden" name="admin_role" value="{{admin_role}}">
    <button type="submit">Edit</button>
    </form>
    </td>
    <input type="hidden" name="id" value="{{id}}">
    <input type="hidden" name="username" value="{{username}}">
    <input type="hidden" name="rold" value="{{role}}">
    <input type="hidden" name="admin_role" value="{{admin_role}}">
    <td class="crud-table highlight">
<form action="/users/delete">
  <input type="hidden" name="id" value="{{id}}">
  <input type="hidden" name="username" value="{{username}}">
  <button type="submit">Delete</button>
</form>
  </td>
  </tr>
    {{/users}}
  </tbody>
</table>
</center>
